<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">

    <select id="cate_count" resultType="int"> <!-- resultType 속성은 반환 자료형 -->
        select count(post_no) from RECIPE_POST
        <where>
         <if test="find_name == ''"></if>
         <if test="find_name != ''">
         title like #{find_name} or ingredient like #{find_name}
         </if>
        </where>
    </select>

    <select id="cate_list" resultType="category">
        select rNum,POST_NO,title,WRITER,score,BOOKMARK,HIT,THUMNAIL,NICKNAME
            from member m right join
            (select rownum rNum,c.POST_NO,c.score,r.TITLE,r.THUMNAIL,r.HIT,r.WRITER,r.BOOKMARK
                from RECIPE_POST r left join
                (select a.post_no, nvl(round(avg(score),2),0) as score
                    from RECIPE_POST a left join RECIPE_REPLY b on(b.post_no=a.POST_NO)
                    group by a.post_no order by a.POST_NO desc)c on (c.POST_NO=r.POST_NO)
        <where>
            <if test="find_name == ''"></if>
            <if test="find_name != ''">title like #{find_name} or ingredient like #{find_name}</if>
        </where>)
        d on (m.USERID=writer)
        where rNum &gt;=#{startrow} and rNum &lt;=#{endrow} order by rnum
<!-- #gt;= 은 >=의 뜻, &lt;= 은 <= 의 뜻 -->
    </select>
</mapper>