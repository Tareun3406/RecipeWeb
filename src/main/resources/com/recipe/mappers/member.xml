<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
    <resultMap id="memberMap" type="member">
        <result property="id" column="id"/>
        <result property="pw" column="pw"/>
        <result property="name" column="name"/>

        <collection property="authList" resultMap="authMap"/>
    </resultMap>
    <resultMap id="authMap" type="auth">
        <result property="id" column="id"/>
        <result property="auth" column="auth"/>
    </resultMap>

    <!-- 회원 정보 + 권한 조회 -->
    <select id="memberSelectAll" resultMap="memberMap">
        select MEMBER.ID,PW,NAME,AUTH from MEMBER left outer join AUTH on MEMBER.ID = AUTH.ID
        where AUTH.ID = #{id}
    </select>
    
    <!-- 회원정보 조회 -->
    <select id="memberSelect" resultType="member">
        select * from member where ID=#{id}
    </select>

    <!-- 회원가입 -->
    <insert id="memberInsert">
        insert into MEMBER VALUES (#{id}, #{pw}, #{name})
    </insert>
    <insert id="authInsert">
        insert into AUTH values (#{id}, #{auth})
    </insert>
</mapper>