<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
    <resultMap id="memberMap" type="member">
        <result property="userid" column="userid"/>
        <result property="userpw" column="userpw"/>
        <result property="nickname" column="nickname"/>

        <collection property="authList" resultMap="authMap"/>
    </resultMap>
    <resultMap id="authMap" type="auth">
        <result property="userid" column="userid"/>
        <result property="auth" column="auth"/>
    </resultMap>

    <!-- 회원 정보 + 권한 조회 -->
    <select id="memberSelectAll" resultMap="memberMap">
        select MEMBER.USERID,USERPW,NICKNAME,AUTH from MEMBER left outer join AUTH on MEMBER.USERID = AUTH.USERID
        where AUTH.USERID = #{userid}
    </select>
    
    <!-- 회원정보 조회 -->
    <select id="memberSelect" resultType="member">
        select * from member where USERID=#{userid}
    </select>

    <!-- 회원가입 -->
    <insert id="memberInsert">
        insert into MEMBER VALUES (#{userid}, #{userpw}, #{nickname})
    </insert>
    <insert id="authInsert">
        insert into AUTH values (#{userid}, #{auth})
    </insert>
</mapper>